# 电票指令


### 作废（只有待发送状态的指令可以进行作废操作： instructionStatus !== '1'）
- 【fms】 https://127.0.0.1/fmselebillinstructions/cancel
- cn.ztessc.controller.bill.FmsEleBillInstructionsCoreController#cancel
```json
{
  "ids": []
}
```

```sql
-- entityList: 查询对应的电票指令
select * from FMS_ELE_BILL_INSTRUCTIONS where id in (:ids)

-- boeIdList = [entityList::boeId]
-- fbbrList: 票据业务单-应收-关联票据表
select * from FMS_BOE_BILL_RECEIVE where BOE_ID in (:boeIdList)



```
