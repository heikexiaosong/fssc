- 1. 使用集团id时， 一定要在UserUtil.getGroupId()  参数中传入业务数据的groupId. ：只要方法可能脱离“用户请求“生命周期，一律显式传参，不要依赖“我确定它是页面调用“
- 2. 在事务方法中， 不要吃掉异常，一定要抛出来， 除非你100%确定， 在事务方法中处理掉异常的时候，本次事务中没有任何sql执行， 否则异常处理掉了， 内层事务（REQUIRED 加入外层）被标记 rollback-only， 然后事务方法退出的时候可能发生 事务提交异常的报错。
- 3. 单据行上的缓存字段数据获取 extendMap.get(key) 千万不要强行指定类型，你不会知道单据上被设置成了什么类型
- 4. 定时器，尽量小批量一个事务， 如果不是所有的数据必须在一个大事务中，最少每页都要一个单独事务

---

# 补充说明与规范修订（2026-09-17 整理）

## 一、对上述 4 条的补充说明与勘误

### 第 1 条补充：没有用户上下文的场景不止定时器

判断标准"只会通过页面调用"过于宽松，以下场景同样没有登录上下文，按原标准容易漏判：

- MQ 消费者（如余额积数补偿 `FmsDataPush#BALANCE_PRODUCT_COMPENSATION`）
- Feign/Dubbo 被其他服务调用（如 `outapi/fmsInnerAccountLevel/query` 这类对外接口）
- 各类回调：银行回调、流程引擎回调、CA 回调
- 运维通过 arthas watch / 手动触发的补偿任务

### 第 2 条补充：rollback-only 的两个推论

- rollback-only 标记只作用于**同一个物理事务**。如果内层用 `REQUIRES_NEW` 开了独立事务，catch 掉异常后外层是可以正常提交的。
- 更隐蔽的坑：catch 之后继续执行的**后续 SQL 最终也会被整体回滚**，造成"代码执行了但数据没进去"的现象，比报错更难排查。

### 第 3 条补充：不要强转，那该怎么做——统一用类型转换工具

extendMap 来自单据行 JSON 反序列化，同一个 key 的数值可能被解析成 `Integer / Long / BigDecimal / String` 任意一种，`(String)`、`(BigDecimal)` 强转都是定时炸弹。

**替代做法**：统一使用 `Convert.toStr(...)`、`ObjUtil.convert(...)`、`StrUtil.toString(...)`、`Convert.toBigDecimal(...)` 这类工具做转换。只禁止不给方案，最后还是会有人写强转。

### 第 4 条补充：小批量事务的三条配套规则

只说"每页一个事务"不够，还需配套：

- **失败页要有记录和补偿机制**：失败页数据被跳过时必须留痕（记录失败页标识/原因），否则丢数无迹可寻。
- **分页查询必须有稳定排序**：统一 `order by xxx asc, id`（末位加唯一列），否则翻页会漏数据或重复处理。
- **幂等筛选 + 防重叠执行**：用状态字段（如 `payment_status='10'`）+ `enabled_flag` 做筛选条件保证可重入；同时要考虑定时器上一轮未跑完、下一轮又启动的重叠执行问题。
- 与第 2 条联动：每页独立事务时，页内失败应抛出并只回滚本页，不要整批中断，更不要整批吞掉。

## 二、新增规范（原文档未覆盖，但系统中实际反复出现的问题）

1. **多集团数据遍历**：定时器处理多集团数据时，groupId 必须随数据行切换并显式传递（含 RPC 调用、缓存 key），不能沿用上一轮循环残留的集团上下文。这是第 1 条在司库多租户场景下的延伸。

2. **软删双标志过滤**：所有查询必须带 `enabled_flag = '0'` 且 `validity_flag = '0'`（按表实际语义）。写新 SQL 漏掉会查到已删除/已失效数据。写 SQL 前先确认该表用哪个（或两个都用）。

3. **状态机更新必须带旧状态条件（乐观锁式 update）**：
   ```sql
   update FMS_PAYMENT_INSTRUCTIONS set payment_status = '30' where id = ? and payment_status = '10'
   ```
   更新影响行数为 0 说明已被并发流转，必须处理而不是忽略。适用于支付指令、对账批次、票据状态等一切状态机。

4. **外系统推单必须以 `externalSystemKey` 做幂等**：入库前先查重或建唯一索引。历史上出现过同一 key 生成多条支付指令的事故（排查 SQL：`group by EXTERNAL_SYSTEM_KEY having count(1) > 1`）。

5. **MQ 消息的幂等与顺序**：消息可能重复消费、乱序到达。消费逻辑必须以数据当前状态为基准做条件更新（同第 3 条），不能假设消息顺序与业务发生顺序一致。余额类补偿消息尤其注意不要用"覆盖写"，要用累加/重算。

6. **IO 操作不要放进 DB 事务**：电子回单下载、附件同步、档案系统上传等网络 IO 混在事务里会拉长事务持锁时间。模式应为：事务内只落库，附件下载在事务外异步补（参考 `fms_electronic_receipt.sync_file_url` 为空时由定时器补下载的设计）。

7. **日期边界统一用半开区间**：`>= '2026-07-01 00:00:00' and < '2026-07-02 00:00:00'`，替代手写 `<= '2026-07-01 23:59:59.999'`。后者容易漏毫秒、写错毫秒位数，跨数据库（达梦/MySQL）毫秒精度不一致时出错。

8. **`UserUtil.getGroupId()` 的调用位置约束**：不仅取值要传业务 groupId，还要注意在循环/流式处理（`stream().map()`）中调用——lambda 内 ThreadLocal 上下文可能已经不同或为空。
