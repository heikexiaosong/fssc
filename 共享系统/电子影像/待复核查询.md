### 待复核查询
- http://192.168.2.135/evssys/imageReview/listImageReviewListFast
```json
{
    "beginUploadDate": "2026-05-09",   
    "endUploadDate": "2026-06-07" ,   
    "reviewStatus": "WAIT_REVIEWED",   
    "imageStatus": "AUDITED"
}
```

```sql
-- 
select U.ID, U.GROUP_ID, egl.NAME as GROUP_NAME, U.EMP_ID, U.EMP_NO , U.LOGIN_NAME, U.GENDER, U.EMAIL, U.MOBILE, U.SUPER_ADMIN , eul.REAL_NAME, U.PASSWORD
from EVS_USERS U
     left join EVS_GROUP_LANGUAGE egl on U.GROUP_ID = egl.GROUP_ID and egl.LANGUAGE = null
     left join EVS_USERS_LANGUAGE eul on U.id = eul.USER_ID and eul.LANGUAGE = null
where U.ENABLED = 'Y'
        and U.EMP_NO = 'XYY057'
        and U.GROUP_ID = '559869b3f454bc9109dc3139322e0058'

-- 
select pse.COMPANY_ID, pr.OPERATION_TYPE_ID
from EVS_PSE_HOLDERS PH
     inner join EVS_POS_STRU_ELEMENTS PSE on PH.POS_STRU_ELEMENT_ID = PSE.ID
     left join EVS_PSE_RELATIONS PR on PH.ID = PR.PSE_HOLDER_ID
where PSE.ENABLED = 'Y'
        and PSE.POS_STRUCTURE_CODE = 'IMAGE_CHECK'
        and PH.EMPLOYEE_ID = 'd73693e823a1191f4b1e3139322e08e2'
        and PSE.GROUP_ID = '559869b3f454bc9109dc3139322e0058'

--
select *
from EID_IMAGE_INFOS EI
where EI.ENABLED = 'Y'
        and EI.IMAGE_STATUS != 'FILING'
        and EI.REVIEW_STATUS is not null
        and EI.GROUP_ID = '559869b3f454bc9109dc3139322e0058'
        and EI.IMAGE_STATUS = 'AUDITED'
        and EI.LAST_UPLOAD_DATE >= TIMESTAMP '2026-05-09 00:00:00.000'
        and EI.LAST_UPLOAD_DATE <= TIMESTAMP '2026-06-07 23:59:59.000'
        and (EI.COMPANY_ID, EI.OPERATION_TYPE_ID) in (('d73693e823abb3acc00a3139322e094b', 'DBE55BA19494273C86ADD78F09E0BAD4'), ('d73693e823ab6a201b783139322e0946', 'DBE55BA19494273C86ADD78F09E0BAD4'))
        and EI.REVIEW_STATUS = 'WAIT_REVIEWED'

```
