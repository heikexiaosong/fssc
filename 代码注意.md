- 1. 使用集团id时， 一定要在UserUtil.getGroupId()  参数中传入业务数据的groupId， 除非你100%确定， 这行代码只会通过页面调用， 不会被定时器调用
- 2. 在事务方法中， 不要吃掉异常，一定要抛出来， 除非你100%确定， 在事务方法中处理掉异常的时候，本次事务中没有任何sql执行， 否则异常处理掉了， 但是异常可能已经被spring默默的记下了， 然后事务方法退出的时候可能发生 事务提交异常的报错。
