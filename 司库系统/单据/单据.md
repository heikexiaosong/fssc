# 单据

#### 待我审批
- https://127.0.0.1/fms/boeList/waitApprove
```sql
select ZBA.ID, ZBH.BOE_NO, ZBA.BOE_ID, ZBH.LE_ID, ZBH.PAY_LE_ID
        , ZBH.PAYMENT_MODE_CODE, ZBH.PAYER_BANK_ACCOUNT_NUM, ZBH.PAYER_BANK_ACCOUNT_NAME, ZBH.PAYER_BANK_BRANCH_NAME, ZBH.PAYER_BANK_ACCOUNT_ID
        , ZBH.GATHER_BANK_ACCOUNT_NAME, ZBH.GATHER_BANK_ACCOUNT_NUM, ZBH.GATHER_BANK_BRANCH_NAME, ZBH.GATHER_BANK_HEAD_NAME, ZBH.SITES_TYPE
        , ZBH.EXPECT_PAYMENT_DATE, ZBH.STRIDE_BANK_SIGN, ZBH.STRIDE_AREA_SIGN, ZBH.URGENT_SIGN, ZBH.TRAD_PURPOSE
        , ZBH.DATA_SOURCE, ZBH.BOE_DATE, ZBH.EMPLOYEE_ID, ZBH.CREATE_BY, ZBH.BOE_ABSTRACT
        , ZBH.PAYMENT_CURRENCY, ZBH.APPLY_AMOUNT, ZBH.BOE_STATUS, ZBH.PROCESS_INSTANCE_ID, ZBH.PAYMENT_STATUS
        , ZBH.CA_PLAIN_DATA, ZBH.CA_SIGNED_DATA, ZBH.CA_AUTH_FLAG, ZBA.APPROVER_USER_CODE, ZBA.TASK_ID
        , ZBA.GROUP_ID, ZBH.BOE_TYPE_CODE, ZBH.FMS_COLLECT_TYPE, ZBH.SOURCE_BOE_NO, ZBH.SOURCE_SYSTEM
        , ZBH.PAYER_BANK_HEAD_CODE, ZBH.ACTUAL_PAYMENT_CURRENCY, ZBH.SETTLEMENT_TYPE, ZBH.UNIQUE_BUS_ID
from FMS_PAYMENT_APPLY_APPROVER ZBA, FMS_PAYMENT_APPLY_HEADER ZBH
where ZBA.BOE_ID = ZBH.ID
        and ZBH.BOE_STATUS = '20'
        and ZBH.ENABLED_FLAG = '0'
        and ZBA.APPROVER_USER_CODE = 'GHG001'
        and ZBH.BOE_TYPE_CODE = 'PAYMENT_BOE'
        and ZBA.group_id = '98384a38c6501ea777273132372e0008'
order by ZBH.boe_date desc, ZBA.id desc
fetch first 20 rows only
```

#### 付款申请单 单据流程
- https://127.0.0.1/fms/core/getFmcHistory
- groupId=98384a38c6501ea777273132372e0008&boeNo=GHG-AP2606150459

```sql
-- 查询付款申请单信息
select *
  from FMS_PAYMENT_APPLY_HEADER
 where boe_No = 'GHG-AP2606150459'
   and group_Id = '98384a38c6501ea777273132372e0008'
```

#### 付款申请单(提交)
- http://127.0.0.1/fms/core/submitDraftBoe

#### 我的已审
- http://127.0.0.1/foe/boeList/myApprove


- http://127.0.0.1/fmc/flow/findMyApprove
```sql
select *
  from (
          select a.task_id, a.type, a.CREATION_DATE as APPROVE_DATE, b.ID, b.APPROVAL_DEPT_CODE
                  , b.SERVICEID_NAME, b.ATTRIBUTE2, b.ATTRIBUTE3, b.ATTRIBUTE4, b.BOE_TYPE_CODE
                  , b.CREATED_BY, b.CREATION_DATE, b.APPLY_USER_CODE, b.ENABLED_FLAG, b.SUBMIT_USER_CODE
                  , b.LAST_UPDATED_BY, b.LAST_UPDATED_DATE, b.COMPANY_CODE, b.OPERATION_TYPE_CODE, b.STATION_ID
                  , b.VENDOR_CODE, b.VERSION, b.STATION_CODE, b.APPROVAL_DEPT_ID, b.BOE_CODE
                  , b.BOE_ABSTRACT, b.PROCESS_ID, b.SYSTEM_CODE, b.BOE_TYPE_NAME, b.CURRENY
                  , b.EMPLOYEE_NAME, b.VENDOR_NAME, b.ATTRIBUTE1, b.ATTRIBUTE5, b.ATTRIBUTE6
                  , b.ATTRIBUTE7, b.ATTRIBUTE8, b.ATTRIBUTE9, b.ATTRIBUTE10, b.OPERATION_TYPE_NAME
                  , b.BOE_STATUS, b.BOE_DATE, b.NODE_ID, b.APPROVED_BY, b.PROCESS_INSTANCE_ID_
                  , b.NODE_NAME, b.GROUP_ID, b.CALL_BACK_URL, b.OPERATION_TYPE_ID, b.POST_ID
                  , b.APPLY_USER_ID, b.BP_COUNT, b.LINE_PROJECT_ID, b.HEADER_PROJECT_ID, b.HEADER_CONTRACT_ID
                  , b.LINE_DEPT_CODE, b.AMOUNT, a.FOCUS_FLAG, b.LE_ID, b.VENDOR_ID
                  , b.FIRST_BOE_DATE, b.BATCH_NUMBER
          from FMC_WF_MY_APPROVED a
                  inner join FMC_WF_BILLS b on a.BUSINESS_ID_ = b.id
                  and b.boe_status <> '1' 
          where a.approved_by_code = 'GHG001'
                  and b.BOE_TYPE_CODE in ('13515', 'TEMPLATE_CLOSE_ACCOUNT_APPLY_FMS0207', 'TEMPLATE_OPEN_ACCOUNT_APPLY_FMS0207', 'OPEN_ACCOUNT_APPLY_FMS_JTPRD', 'UPDATE_ACCOUNT_APPLY_FMS_JT', 'OPEN_ACCOUNT_APPLY_FMS_JT', 'CLOSE_ACCOUNT_APPLY_FMS_JT')
                  and b.SYSTEM_CODE = 'FSSC-FOE'
                  and b.GROUP_ID = '98384a38c6501ea777273132372e0008'
                  and b.BOE_CODE not like '%ZTE_PRE'
          union all
          select a.task_id, a.type, a.CREATION_DATE as APPROVE_DATE, b.ID, b.APPROVAL_DEPT_CODE
                  , b.SERVICEID_NAME, b.ATTRIBUTE2, b.ATTRIBUTE3, b.ATTRIBUTE4, b.BOE_TYPE_CODE
                  , b.CREATED_BY, b.CREATION_DATE, b.APPLY_USER_CODE, b.ENABLED_FLAG, b.SUBMIT_USER_CODE
                  , b.LAST_UPDATED_BY, b.LAST_UPDATED_DATE, b.COMPANY_CODE, b.OPERATION_TYPE_CODE, b.STATION_ID
                  , b.VENDOR_CODE, b.VERSION, b.STATION_CODE, b.APPROVAL_DEPT_ID, b.BOE_CODE
                  , b.BOE_ABSTRACT, b.PROCESS_ID, b.SYSTEM_CODE, b.BOE_TYPE_NAME, b.CURRENY
                  , b.EMPLOYEE_NAME, b.VENDOR_NAME, b.ATTRIBUTE1, b.ATTRIBUTE5, b.ATTRIBUTE6
                  , b.ATTRIBUTE7, b.ATTRIBUTE8, b.ATTRIBUTE9, b.ATTRIBUTE10, b.OPERATION_TYPE_NAME
                  , b.BOE_STATUS, b.BOE_DATE, b.NODE_ID, b.APPROVED_BY, b.PROCESS_INSTANCE_ID_
                  , b.NODE_NAME, b.GROUP_ID, b.CALL_BACK_URL, b.OPERATION_TYPE_ID, b.POST_ID
                  , b.APPLY_USER_ID, b.BP_COUNT, b.LINE_PROJECT_ID, b.HEADER_PROJECT_ID, b.HEADER_CONTRACT_ID
                  , b.LINE_DEPT_CODE, b.AMOUNT, a.FOCUS_FLAG, b.LE_ID, b.VENDOR_ID
                  , b.FIRST_BOE_DATE, b.BATCH_NUMBER
          from FMC_WF_MY_APPROVED a
                  inner join FMC_WF_HIS_BILLS b on a.BUSINESS_ID_ = b.id 
          where a.approved_by_code = 'GHG001'
                  and b.BOE_TYPE_CODE in ('13515', 'TEMPLATE_CLOSE_ACCOUNT_APPLY_FMS0207', 'TEMPLATE_OPEN_ACCOUNT_APPLY_FMS0207', 'OPEN_ACCOUNT_APPLY_FMS_JTPRD', 'UPDATE_ACCOUNT_APPLY_FMS_JT', 'OPEN_ACCOUNT_APPLY_FMS_JT', 'CLOSE_ACCOUNT_APPLY_FMS_JT')
                  and b.SYSTEM_CODE = 'FSSC-FOE'
                  and b.GROUP_ID = '98384a38c6501ea777273132372e0008'
                  and b.BOE_CODE not like '%ZTE_PRE'
  ) t
  where GROUP_ID = '98384a38c6501ea777273132372e0008'
  order by FOCUS_FLAG desc, APPROVE_DATE desc, ID desc

-- 生成流程上一个审批节点我的已审记录， 例如： <单据号> 替换成 'GHG-FX2606150002', <最后一个节点名名称> 替换成 '结算中心审批'
INSERT INTO ZFS_FMC.FMC_WF_MY_APPROVED (ID, VERSION, BUSINESS_ID_, APPROVED_BY_CODE, CREATED_BY, CREATION_DATE, LAST_UPDATED_BY
, LAST_UPDATED_DATE, ENABLED_FLAG, APPROVED_BY, TASK_ID, TYPE, FOCUS_FLAG)
SELECT  LOWER(RAWTOHEX(sys_guid())), '0', t.BUSINESS_ID_, t.ASSIGNEE_, null, t.CREATE_DATE_, t.CREATE_DATE_, 'Y', null, t.id_, 'Normal', 'N'
FROM ZFS_FMC.FMC_UFLO_HIS_TASK t
         left join FMC_WF_HIS_BILLS b on t.BUSINESS_ID_ = b.id
WHERE SUBJECT_ = '<单据号>' and NODE_NAME_ = '<最后一个节点名名称>';

```
