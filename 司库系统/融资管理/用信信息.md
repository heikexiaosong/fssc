# 融资预算管理 > 额度使用查询

## 额度使用查询列表
- http://127.0.0.1/innerLoanQuota/queryPageForQuery/page
```json
{ "query": [{"code":"usedCreditNo","condition":3,"fieldType":1,"value":"RZYX260700003"}] }
```

1. 授权类型: 10【融资管理模块】
2. 表：FMS_GUARANTEE_QUOTA


## 占用额度明细
- http://127.0.0.1/fmsCreditUsageRecord/queryPage
```json
{ "quotaId_EQ": "3d6c9d0bb8fe5be2a28231bb8fe5be"}
```

1. 授权类型: 无
2. 表：FMS_CREDIT_USAGE_RECORD

## 额度使用详情
- http://127.0.0.1/innerLoanQuota/3d6c9d0bb8fe5be2a28231bb8fe5be


1. 授权类型: 无
2. 表：FMS_INNER_LOAN_QUOTA
3. 用信品种【关联信息】：
```sql
   select * from FMS_INNER_LOAN_QUOTA_BREED where QUOTA_ID = FMS_INNER_LOAN_QUOTA::ID  -- 用信额度分类
   select * from fms_inner_loan_quota_detail where QUOTA_ID = FMS_INNER_LOAN_QUOTA::ID -- 用信额度明细
   -- FmsInnerLoanQuotaDetailDTO::BreedId[用信品种ID] == FmsInnerLoanQuotaBreedDTO::ID
```

## 新增额度
- http://127.0.0.1/innerLoanQuota
FMS_INNER_LOAN_QUOTA: 内部借款额度台账新增
FMS_INNER_LOAN_QUOTA_BREED： 用信额度分类表新增
fms_inner_loan_quota_detail：用信额度明细实体类新增
2、保存附件
FMS_ATTACHMENT: 附件信息保存
3. 
FMS_INNER_LOAN_QUOTA_RECORD： 内部借款额度记录新增
FMS_USED_CREDIT_SHARE_LE： 用信共享核算主体表新增


```sql
-- 在途
SELECT t.* 
  FROM zfs_fms.fms_quota_transit_detail t
 WHERE QUOTA_ID = '346099b5403fc47741643137322e6316'

-- 冻结
SELECT t.* 
  FROM zfs_fms.fms_credit_frozen_record t
 WHERE QUOTA_ID = '346099b5403fc47741643137322e6316'

 -- 占用
SELECT t.* 
  FROM zfs_fms.fms_credit_usage_record t
 WHERE QUOTA_ID = '346099b5403fc47741643137322e6316'
 
```
