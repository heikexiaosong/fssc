# 代付申请单

```js
<zte-button
  type="text"
  v-if="
    (row.paymentStatus === '24' && row.dataSource !== '1')
      || (row.paymentStatus === '12' && row.dataSource === '1')
  "
  :disabled="row.businessType === 'REFUND_REQUEST_BOE'"
  @click="handleRepay(row)"
>
  {{ $t('重付申请单') }}
</zte-button>
````

### 重复申请单
- https://127.0.0.1/core/repeatBoe
```json
{
  "boeNo": "HUASHI-AP2607130001",
  "boeHeaderId": "fce648b5-e754-446c-a0db-c9585590c36b",
  "key": "8LaGSLtPvM37o2JiyuNSwuaX3Q7lwykexaHf2tT7vKY="
}
```

```sql
-- 更新单据对应的指令状态 从 12/24 到 13/40
-- externalSystemKey = UuidUtils.getUuid()
update FMS_PAYMENT_INSTRUCTIONS set PAYMENT_STATUS =:paymentStatus, EXTERNAL_SYSTEM_KEY =:externalSystemKey, REPEAT_SEND_COUNT =:repeatSendCount where BOE_NO = 'HUASHI-AP2607130001' and PAYMENT_STATUS in ('12', '24')
```

-- 新旧单据关联
new.repeatBoeNo = old.boeNo;
old.newBoeNo = new.boeNo;



#### 导入工资明细
- http://127.0.0.1/fmsPayRoll/importPayRoll?boeId=13da7d20-ec61-4bd6-8c4c-02b18aef9912
- boeId_EQ=13da7d20-ec61-4bd6-8c4c-02b18aef9912
- file=<明细文件>
```excel
bankName	bankUnitNumber	payRollAmount	crossLineIdentification	wageUse	remark
收款方户名	收款方开户行号	    金额	          跨行标识	              用途	    备注
依依	    102100099996	    3.17	        0		
```

```python

//付款方总行
String payerBankHeadCode = fmsPaymentApplyHeaderDTO.getPayerBankHeadCode();

// 1. 复制对象（基础数据）         
canImport = ConvertUtils.sourceToTarget(dto, FmsPayRollDTO.class)
    
// 2.set开户行名称(dto.getBankUnitNumber())
canImport.setBankUnitName(bankCacheDTO.getName());

// 3. 比较付款方银行行号与收款方开户行行号，设置跨行标识
// - 需求： https://www.tapd.cn/tapd_fe/67410840/story/detail/1167410840001331803
if (StrUtil.equals(bankUnitedCodeDTO.getBankTypeCode(), payerBankHeadCode)) {
    canImport.setCrossLineIdentification(FmsEnumType.CROSS_LINE_IDENTIFICATION.SAME_BNAK.getName());
} else {
    canImport.setCrossLineIdentification(FmsEnumType.CROSS_LINE_IDENTIFICATION.CROSS_BANK.getName());
}

// 4. set工资金额
canImport.setPayRoll(new BigDecimal(payRollAmount));

// 5. set工资金额
canImport.setId(UuidUtils.getUuid());

// 6. set代发工资单据id
canImport.setBoeId(boeId);

// 7. set明细单号
String orderNumber = String.format(FmsConstants.DETAILED_ORDER_NUMBER_FORMAT, ++maxDetailedOrderNumber);
canImport.setDetailedOrderNumber(orderNumber);
canImport.setBankOrderNumber(orderNumber);

// 8. set创建人
EntityUtils.setWho(canImport);
```

```sql


-- 更新单据的 总笔数 和 总金额
update FMS_PAYMENT_APPLY_HEADER set TOTAL_COUNT = :totalCount, APPLY_AMOUNT = :applyAmount where id =:id
```


### 付款账号选择(华西)
-【fms】http://127.0.0.1/fmsaccountPrj/pageForBoePrj
```json
{
  "paymentModeCode": "2",  
  "currencyCode": "CNY",  
  "transLegalPerson": "82135b6777a92c84e13fb1b7b2ae12a9"
}
```

```sql
--
select *
from fms_account fav
where 1 = 1
  and fav.sales_mark in ('0', '3')
  and (fav.freezing_mark is null
    or fav.freezing_mark = '0')
  and fav.account_attribute in ('2', '3')
  and fav.group_id = 'af4ce58b58c721b9dcde3137322e0004'
  and fav.validity_flag = '0'
  and fav.sales_mark != '1'
  and exists(
        select fac.currency_code
        from fms_account_currency fac
        where fav.id = fac.account_id
          and fac.ENABLED_FLAG = '0'
          and fac.currency_code = 'CNY'
    )
  and fav.legal_Person = '20b7867c1e3600edc107b1b7b2ae3727'
  and ((fav.id in ('357d57a615b15f7315fc3137322e044a')
    and fav.le_id in ('82135b6774e3bd571dd5b1b7b2ae040f')
    and exists(
                select fpr.le_id
                from fms_pool_org fpo
                         inner join fms_pool_reorg fpr on fpr.ORG_ID = fpo.id
                where fpo.ENABLED_FLAG = '0'
                  and fpo.VALIDITY_FLAG = '0'
                  and fpr.ENABLED_FLAG = '0'
                  and fpr.VALIDITY_FLAG = '0'
                  and fpr.le_id = fav.le_id
                  and (fpo.id in ('aa5cbaa2ffffdb8a9997be34b1b7b2ae0e57')
                    or fpo.FID like '/aa5cbaa2ffffd3d81bb634e7b1b7b2ae0c43/aa5cbaa2ffffdb8a9997be34b1b7b2ae0e57%')
            )) or exists(
               select fpr.le_id
               from fms_pool_org fpo
                        inner join fms_pool_reorg fpr on fpr.ORG_ID = fpo.id
               where fpo.ENABLED_FLAG = '0'
                 and fpo.VALIDITY_FLAG = '0'
                 and fpr.ENABLED_FLAG = '0'
                 and fpr.VALIDITY_FLAG = '0'
                 and fpr.le_id = fav.le_id
                 and (fpo.id in ('aa5cbaa2ffffdb8a9997be34b1b7b2ae0e57')
                   or fpo.FID like '/aa5cbaa2ffffd3d81bb634e7b1b7b2ae0c43/aa5cbaa2ffffdb8a9997be34b1b7b2ae0e57%')
           ))
order by fav.create_date desc, fav.last_update_date, fav.id desc
limit 10
```
