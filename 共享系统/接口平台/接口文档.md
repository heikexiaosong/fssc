# 接口文档


### 刷新
- 【interface-internal】http://127。0.0.1/apiDoc/syncDocument?serviceCode=ALL
-  cn.ztessc.controller.platform.ApiDocumentNewController#syncDocument
```json
{
  "serviceCode": "ALL"
}
```

[nacos] api.yml
"http://%s/v3/api-docs/outer-api".format(serviceCode)

[nacos] common.yml
knife4j:
  # 开启Swagger的Basic认证功能,默认是false
  basic:
    enable: true
    # Basic认证用户名
    username: admin
    # Basic认证密码
    password: test
