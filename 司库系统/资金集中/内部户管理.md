### 业务查询 > 余额查询
- http://127.0.0.1/sys/fms/fmsInnerBalance/page
```json
{ 
  "startRegisterDate": "2026-01-01",
  "endRegisterDate": "2026-05-25",
  "bankNumber": "0102201",
}
```

```sql
-- 权限查询
select t0.ID, t0.ORG_PERMISSION_TYPE, t0.SITES_TYPE, t0.MODE_ALL_FALG, t0.MENU_ALL_FALG
        , t0.LE_ALL_FALG, t0.ACCOUNT_ALL_FALG, t0.ORG_ALL_FALG, t0.BELONG_LE_ALL_FALG
from fms_account_auth_config t0, fms_auth_config_emp t1, fms_auth_config_type t2
where t0.id = t1.auth_config_id
        and t1.auth_config_id = t2.auth_config_id
        and t1.emp_id = '36881a8cf915dd72f5b93137322e0da4'
        and t2.auth_type = '13'
        and t0.enabled_flag = '0'
        and t0.validity_flag = '0'
group by t0.ID, t0.ORG_PERMISSION_TYPE, t0.SITES_TYPE, t0.MODE_ALL_FALG, t0.MENU_ALL_FALG, t0.LE_ALL_FALG, t0.ACCOUNT_ALL_FALG, t0.ORG_ALL_FALG, t0.BELONG_LE_ALL_FALG

-- 余额查询
select a.*, ia.account_name, ia.bank_number, ia.le_id, ia.account_type
        , ia.overdraw_flag, ia.interest_flag, ia.account_state
from fms_inner_balance a
        inner join fms_inner_account ia on a.account_id = ia.id
where 1 = 1
        and exists (
                select fpr.le_id
                from fms_pool_org fpo
                        inner join fms_pool_reorg fpr on fpr.ORG_ID = fpo.id
                where fpo.ENABLED_FLAG = '0'
                        and fpo.VALIDITY_FLAG = '0'
                        and fpr.ENABLED_FLAG = '0'
                        and fpr.VALIDITY_FLAG = '0'
                        and fpr.le_id = ia.le_id
                        and (fpo.id in ('6f0f10ab765230d139d03137322e3206', 'ddf8cd7c07be1f03b0c83137322e1b37')
                                or fpo.FID like '/2cfff7a23ba225f47a773137322e1139/6f0f10ab765230d139d03137322e3206%')
        )
        and a.group_id = '98384a38c6501ea777273132372e0008'
        and a.enabled_flag = '0'
        and a.register_date >= TIMESTAMP '2026-01-01 00:00:00.000'
        and a.register_date < TIMESTAMP '2026-05-26 00:00:00.000'
        and ia.bank_number like '%0102201%'
        and (ia.cancel_date is null
                or a.register_date <= ia.cancel_date)
order by ia.bank_number, a.register_date desc
fetch first 20 rows only
```

### 重算余额
- http://127.0.0.1/sys/fms/fmsInnerBalance/reBalance
```json
{
    "accountId": "4ef6263bec949039564d3137322e0b4c",
    "accountName": "广东广新置业发展有限公司",
    "date": "2026-03-21",
    "openDate": "2009-10-16"
}
````

```sql
-- 获取内部户信息 accountEntity
select *
from FMS_INNER_ACCOUNT
where id = '4ef6263bec949039564d3137322e0b4c'

-- throw Exception(date < accountEntity:lastInterestDate>)

-- 获取日期内内部户余额积数最大日期记录 initAmount
select t.* 
  from fms_inner_balance  t 
    right join (
      select ACCOUNT_ID,max(REGISTER_DATE) as REGISTER_DATE from fms_inner_balance fib where 1=1
      and fib.ACCOUNT_ID = :accountId
      and fib.REGISTER_DATE < :startDate
      and fib.enabled_flag = '0'
      group by fib.ACCOUNT_ID ) tmp on t.ACCOUNT_ID=tmp.ACCOUNT_ID and t.REGISTER_DATE= tmp.REGISTER_DATE
  where t.ACCOUNT_ID = :accountId
    and t.enabled_flag = '0'

-- 判断开始日期是否是余额日期的下一天，不是下一天要累加进来
-- accountCurrencyListByAccountId
select *
  from FMS_INNER_CURRENCY
 where ACCOUNT_ID = :accountId
   and enabled_flag = '0'


-- 查询已存在的余额积数
-- afterList   ->  map: key=registerDateNum, value=balance
select *
  from FMS_INNER_BALANCE
 where ACCOUNT_ID = :accountId
   and REGISTER_DATE >= :startDate
   and REGISTER_DATE < :endDate
   and enabled_flag = '0'


```


### 内部户开户申请单
开户单位: https://127.0.0.1/fmsPoolReorg/pageValid
```json
{
  "leId": "26ecc79ae5b753edd0473137322e571e",
  "orgType": "2",
  "authType": "13"
  
}
```


### 内部户登记
-【POST】 http://127.0.0.1/fmsAccountGroupConfig/saveInnerAccount



### 利息管理 > 利息预提
-【FMS】 https://127.0.0.1/fmsInnerInterrateRecord/pageInterestWithholding
- cn.ztessc.controller.inneraccount.FmsInnerInterrateRecordController#pageInterestWithholding
```json
{
  "maxWithholdDate": "2026-07-20"
}
```
```sql
--  
select *
  from fms_inner_account fav
 where 1 = 1
	 and fav.group_id = 'af4ce58b58c721b9dcde3137322e0004'
	 and fav.bank_number like '%HBF2102009-01%'
	 and fav.enabled_flag = '0'
	 and fav.account_type in ('01', '04')
	 and MAX_WITHHOLD_DATE < TIMESTAMP '2026-07-20 00:00:00.000'
	 and fav.interest_flag = '0'
	 and fav.AGREEMENT_FLAG = '1'
	 and (fav.COMPLETION_FLAG is null	or fav.COMPLETION_FLAG = '2')
		and (fav.CANCEL_DATE is null or fav.CANCEL_DATE >= MAX_WITHHOLD_DATE)
	order by bank_number asc
	limit 20
```




### 利息管理 > 存款结息
- 【FMS】 https://127.0.0.1/fmsInnerInterrateRecord/pageInterestSettlement
- cn.ztessc.controller.inneraccount.FmsInnerInterrateRecordController#pageInterestSettlement
```json
{
  "maxInterestDate": "2026-07-21"
}
```

```sql

-- 
select fav.*, fav.INTEREST_START_DATE as INTEREST_Settlement_START_DATE
  from fms_inner_account fav
 where 1=1
	 and fav.group_id = 'af4ce58b58c721b9dcde3137322e0004'
	 and fav.bank_number like '%HBF2102009-01%' escape '\\'
	 and fav.enabled_flag = '0'
	 and fav.account_type in ('01', '04')
	 and MAX_INTEREST_DATE < TIMESTAMP '2026-07-21 00:00:00.000'
	 and (fav.CANCEL_DATE is null or CANCEL_DATE > TIMESTAMP '2026-07-21 00:00:00.000')
	 and (fav.BILLING_DAY is null or fav.BILLING_DAY <> '21')
	 and fav.interest_flag = '0'
	 and fav.AGREEMENT_FLAG = '1'
	 and (fav.COMPLETION_FLAG is null or fav.COMPLETION_FLAG = '2')
	 and (fav.CANCEL_DATE is null or fav.CANCEL_DATE >= MAX_INTEREST_DATE)
	order by bank_number asc
	limit 20

	-- 
	select *
	from FMS_INNER_INTERRATE_RECORD t
		right join (
			select INNER_ID, max(EFFECTIVE_DATE) as EFFECTIVE_DATE
			from FMS_INNER_INTERRATE_RECORD t
			where 1 = 1
				and t.INNER_ID = '5f5b0449ffffa9224ac68114b1b7b2ae4124'
				and t.EFFECTIVE_DATE < TIMESTAMP '2026-06-20 00:00:00.000'
				and t.enabled_flag = '0'
			group by t.INNER_ID
		) tmp
		on t.INNER_ID = tmp.INNER_ID
			and t.EFFECTIVE_DATE = tmp.EFFECTIVE_DATE
			and t.INNER_ID in ('5f5b0449ffffa9224ac68114b1b7b2ae4124')
			and t.enabled_flag = '0'
	order by t.EFFECTIVE_DATE asc
```


### 【定时器】 存款结息
- cn.ztessc.service.task.JobCenter#executeDepositSettlement
```sql
-- 账户数量
select count(1)
  from fms_inner_account fav
 where 1 = 1
   and fav.enabled_flag = '0'
   and fav.account_type in ('01', '04') -- 账户类型
   and MAX_INTEREST_DATE < TIMESTAMP '2026-08-12 00:00:00.000'
   and fav.interest_flag = '0' -- 是否计息
   and fav.AUTO_ACCRUAL = '0' -- 是否自动结息 0-是，1-否
   and fav.AGREEMENT_FLAG = '1' -- 是否协定 
   and (fav.CANCEL_DATE is null or fav.CANCEL_DATE >= MAX_INTEREST_DATE)
   and fav.BILLING_MONTH like '%,8,%'
   and fav.BILLING_DAY = '12'

```


### 【定时器】计算余额积数
- cn.ztessc.service.task.JobCenter#reBalanceInnerAccount
```sql
-- 

```

### 活期利息管理 > 利息计算
- 【fms】 https://127.0.0.1/fmsInnerInterrateRecord/pageInterestCalculate
- cn.ztessc.service.task.JobCenter#reBalanceInnerAccount
```json
{
  "interestCalcStartDate": "2026-07-28",
  "interestCalcEndDate": "2026-08-27",
  "orgPermissionType": "2",
  "accountType": "01^04",
  "agreementFlag": "1",
  "interestFlag": "0",
  "completion": "N"
}
```

```sql
--
select *
from fms_inner_account fav
where 1 = 1
  and fav.group_id = 'af4ce58b58c721b9dcde3137322e0004'
  and fav.bank_number like '%HB01018001-01%' 
  and fav.enabled_flag = '0' 
  and fav.account_type in ('01', '04') 
  and INTEREST_START_DATE < TIMESTAMP '2026-08-27 00:00:00.000' 
  and fav.interest_flag = '0' 
  and fav.AGREEMENT_FLAG = '1' 
  and (fav.COMPLETION_FLAG is null or fav.COMPLETION_FLAG = '2') 
  and (fav.CANCEL_DATE is null or fav.CANCEL_DATE >= TIMESTAMP '2026-07-28 00:00:00.000') 
  and (fav.CANCEL_DATE is null or fav.CANCEL_DATE >= INTEREST_START_DATE)
order by bank_number asc limit 20


-- 查询leId对应的资金组织信息
select fpr.le_id,
       fpr.org_id,
       l1.org_name,
       fpo.code,
       fpr.le_id
from fms_pool_org fpo
         inner join fms_pool_reorg fpr
                    on fpo.id = fpr.org_id
         left join fms_pool_org_language l1 on fpo.ID = l1.ORG_ID and l1.LANGUAGE = 'zh-CN'
where 1=1
  -- and fpo.id = 'd2a52ad45f4f09ce5505b1b7b2ae1ae1'
  and fpr.le_id in ('e088f27affff9ee7403023f8b1b7b2ae43b2')
  and fpo.validity_flag = '0'
  and fpr.validity_flag = '0'
  and fpr.enabled_flag = '0'
  and fpr.org_type = '2'
  and fpr.group_id = 'af4ce58b58c721b9dcde3137322e0004'

```
